
/*class joyas{
	constructor(nombre,precio,motivo,stock){

		this.nombre = nombre;
		this.precio = precio;
		this.motivo = motivo;
		this.stock = stock;

	}
	promocion(){
		let promocion = `Con la compra de 2 ${this.nombre}, te hacemos 10% de descuento. 
		En lugar de pagar ${this.precio*2} pagarias ${(this.precio*2)-(this.precio*2/10)}`
		console.log(promocion)
	}

	ventas(){
		if(this.stock >0){
			console.log(`Confirmamos la compra de ${this.nombre}`)
				this.stock = this.stock - 1
		}else{
			let error = `Sin stock disponible de ${this.nombre}`
			console.log(error)
		}
		
	}

}

const aros = new joyas("aros", 1500,"Plata",2)
const pulceras = new joyas("pulceras", 3700,"Oro",1)
const anillos = new joyas("anillo", 1200,"acero",3)



aros.promocion()
aros.ventas()
pulceras.promocion()
pulceras.ventas()
pulceras.promocion()
pulceras.ventas()
anillos.promocion()
anillos.ventas()
aros.promocion()
aros.ventas()
aros.promocion()
aros.ventas()

*/

